image: openjdk:8

stages:
  - build

build:
  stage: build
  script:
    - chmod +x gradlew
    - ./gradlew clean build

deploy:
  stage: deploy
  script:
    - chmod +x cicd/deploy.sh
    - ./cicd/deploy.sh
  dependencies:
    - build
